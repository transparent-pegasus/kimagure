# UI/UXのさらなる洗練とデザイン調整

## 概要
`docs/todo.md` に記載された残りのUI改善項目と、デザインの細部調整を行う。特に動物アイコンの導入、入力欄のプレースホルダーの具体化、および結果画面のブラッシュアップに注力する。

## 変更内容

### 1. デザイン素材の生成
- [x] タイトルの「きまぐれごはん」に合わせた、可愛らしい動物（リスやクマなど）のロゴアイコンを生成。
- [x] 各セクションの見出しに使用する動物アイコン（料理をする動物、食材を持つ動物など）を検討。

### 2. ページ構成と初期状態の調整 (`frontend/app/page.tsx`)
- **食事プランセクション**:
  - [x] 紹介文を「今日食べたもの・食べる予定のものを入力するか、<br>「提案」を選んでね」に変更し、サイズを `text-sm` に調整。
  - [x] `MealInputRow` に「提案」ラベル付きのチェックボックスを配置。
  - [x] 各入力欄のプレースホルダーを「おにぎり（セブンイレブン）」「サラダ」「お弁当」などの具体的な内容に変更。
  - [x] 提案対象選択時の表示を「選択中」のみにし、文字色を通常時と同等に変更。
- **提案条件セクション**:
  - [x] 「使いたい材料」のプレースホルダーを「食べたい料理」と合わせる。
  - [x] 「食べたい料理」のプレースホルダーを「和定食」「ハンバーグセット」などのセットメニュー形式にする。
- **ボリューム・構成セクション**:
  - [x] `volume` 状態を追加し、選択ボタンがクリックで状態変更できるように実装。
- **設定・NG食材セクション**:
  - [x] `糖質`, `砂糖`, `油`, `食塩相当量` をデフォルトの制限項目として、制限なし（スイッチOFF）状態で初期表示する。
  - [x] 全てのNG項目のプレースホルダーを「食材を入力」に統一。
  - [x] NGボタンの配置を数値入力欄の左（順序入れ替え）にし、サイズを調整。
- **アクションボタン**:
  - [x] 「献立を決める！」ボタンからSVGアイコンを削除し、テキストのみにする。

### 3. 提案結果画面の調整 (`frontend/app/page.tsx`)
- **タイトル**: 記号を排除し「今日の夜ごはん」などの形式に修正。
- **食事リスト**: 「(未入力)」を「未入力」に変更。
- **ボタン**:
  -  [x] 「条件を変える」ボタンを削除。
  - [x] 「これで決定！」を「保存する」に変更し、色を濃い黄色（amber-500〜600系）に調整。

### 4. コンポーネント更新
- `frontend/components/layout/Header.tsx`: 生成したロゴを配置し、動物アイコンを導入。
- `frontend/app/page.tsx` 内の `Section` コンポーネント: 見出しのアイコンを動物関連のものに変更（lucide-reactの代替または生成画像）。

## 完了定義
- [x] `docs/todo.md` の全項目が反映されていること。
- [x] `make verify` または `pnpm build` が正常に終了すること。
- [x] デザインが一貫性を持ち、親しみやすい「動物×料理」のテーマになっていること。
